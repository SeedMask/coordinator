# Track rusty-kaspa mass/fee rules (Toccata); requires v2.0.1+ for post-June 2026 mainnet.
kaspa>=2.0.1
embit>=0.8.0
qrcode[pil]>=8.0
Pillow>=10.0.0
# OpenCV is not shipped in the desktop runtime (Electron uses jsqr for camera QR).
# Optional local-only: pip install opencv-python-headless  # for app/qr_scanner.py

# FastAPI backend (Electron-bundled + optional browser UI)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
websockets>=13.0
cryptography>=42.0.0
