.DS_Store
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Node / Electron
node_modules/
electron/node_modules/
electron/out/
electron/release/
electron/dist/
# Bundled runtimes are OS-specific and rebuilt in CI / release scripts.
electron/build/runtime/
electron/*.tsbuildinfo
.cache/
*.log

# Keep app icons + mac entitlements in git (needed for CI packaging).
# Do not ignore electron/build/ entirely.

# Installers / binaries (publish via GitHub Releases)
*.dmg
*.exe
*.AppImage
*.deb
*.rpm

# Local / bulky vendored archives (fetch via setup script)
tools/kaspa_wasm_node/sdk.zip
tools/kaspa_wasm_node/kaspa-wasm.tgz
tools/kaspa_wasm_node/kaspa-wasm32-sdk-*.zip

# Old local download staging (site lives at SeedMask/seedmask.io)
website/downloads/
electron/.cache/
